# Road Condition Indexer - Environment Configuration Template
# Copy this file to '.env' and fill in your values

# =================================================================
# DATABASE CONFIGURATION
# =================================================================
# Azure SQL Server Configuration (Primary Database Backend)
# If these variables are provided, the application will use Azure SQL Server
# If any are missing, the application will automatically fall back to SQLite

AZURE_SQL_SERVER=your-server.database.windows.net
AZURE_SQL_DATABASE=your-database-name
AZURE_SQL_USER=your-username
AZURE_SQL_PASSWORD=your-password
AZURE_SQL_PORT=1433

# =================================================================
# AZURE MANAGEMENT (OPTIONAL)
# =================================================================
# These are only required if you want to use the /manage endpoints
# for Azure App Service and SQL Database management

AZURE_SUBSCRIPTION_ID=your-subscription-id
AZURE_TENANT_ID=your-tenant-id
AZURE_CLIENT_ID=your-client-id
AZURE_CLIENT_SECRET=your-client-secret
AZURE_RESOURCE_GROUP=your-resource-group
AZURE_APP_PLAN_NAME=your-app-service-plan
AZURE_APP_NAME=your-app-service-name

# =================================================================
# APPLICATION SETTINGS (OPTIONAL)
# =================================================================
# Fine-tune application behavior

# Data filtering thresholds
RCI_MAX_INTERVAL_SEC=15      # Maximum time between data points (seconds)
RCI_MAX_DISTANCE_M=100       # Maximum distance between data points (meters)  
RCI_MIN_SPEED_KMH=7          # Minimum speed for data recording (km/h)

# =================================================================
# NOTES
# =================================================================
# 
# DATABASE FALLBACK:
# - If Azure SQL variables are incomplete, SQLite will be used automatically
# - SQLite database file: RCI_local.db (created automatically)
# - No additional configuration needed for SQLite
#
# DEPLOYMENT:
# - For production: Use Azure SQL Server configuration
# - For development: Can omit Azure SQL variables to use SQLite
# - For testing: SQLite is recommended for simplicity
#
# SECURITY:
# - Never commit the actual .env file to version control
# - Use Azure Key Vault or App Service Configuration for production secrets
# - Rotate passwords and secrets regularly
