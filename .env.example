# Road Condition Indexer - Local Development Environment Configuration
# Copy from .env.template and fill in your values

# =================================================================
# DATABASE CONFIGURATION (REQUIRED)
# =================================================================
# Azure SQL Server Configuration
# All variables are required for SQL Server connectivity

# Example: your-server.database.windows.net
AZURE_SQL_SERVER=your-server.database.windows.net

# Example: your-database-name
AZURE_SQL_DATABASE=your-database-name

# Example: your-username
AZURE_SQL_USER=your-username

# Example: your-password
AZURE_SQL_PASSWORD=your-password

# Standard SQL Server port
AZURE_SQL_PORT=1433

# =================================================================
# AZURE MANAGEMENT (OPTIONAL)
# =================================================================
# These are only required if you want to use the /manage endpoints
# for Azure App Service and SQL Database management

# AZURE_SUBSCRIPTION_ID=your-subscription-id
# AZURE_TENANT_ID=your-tenant-id
# AZURE_CLIENT_ID=your-client-id
# AZURE_CLIENT_SECRET=your-client-secret
# AZURE_RESOURCE_GROUP=your-resource-group
# AZURE_APP_PLAN_NAME=your-app-service-plan
# AZURE_APP_NAME=your-app-service-name

# =================================================================
# APPLICATION SETTINGS (OPTIONAL)
# =================================================================
# Fine-tune application behavior

# Data filtering thresholds
# RCI_MAX_INTERVAL_SEC=15      # Maximum time between data points (seconds)
# RCI_MAX_DISTANCE_M=100       # Maximum distance between data points (meters)  
# RCI_MIN_SPEED_KMH=7          # Minimum speed for data recording (km/h)

# =================================================================
# NOTES
# =================================================================
# 
# PRODUCTION DEPLOYMENT:
# - In Azure App Service, set these as Application Settings instead of using .env file
# - The application automatically detects Azure environment and uses App Settings
# - SQL Server connection is mandatory - no SQLite fallback available
#
# LOCAL DEVELOPMENT:
# - Rename this file to '.env' and fill in your SQL Server details
# - Install python-dotenv: pip install python-dotenv
# - The application will automatically load these variables on startup
#
# SECURITY:
# - Never commit .env files with real credentials to version control
# - Add .env to your .gitignore file
# - Use Azure Key Vault for production secrets
