############################################################
# Road Condition Indexer - Example .env file               #
# Copy to .env and fill in real secret(s).                 #
############################################################

# Database connection
AZURE_SQL_PASSWORD=REPLACE_ME
AZURE_SQL_SERVER=erikmaa.database.windows.net
AZURE_SQL_DATABASE=rci-nl-database
AZURE_SQL_USER=techno
AZURE_SQL_PORT=1433

# Application thresholds
RCI_MAX_INTERVAL_SEC=15
RCI_MAX_DISTANCE_M=100
RCI_MIN_SPEED_KMH=7

# Notes:
# - Non-secret metadata and structured docs moved to .config-example.
# - Never commit real secrets to version control.
